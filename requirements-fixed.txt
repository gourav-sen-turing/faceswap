# Core Dependencies (Fixed versions for Docker build)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Computer Vision & Deep Learning (Pre-compiled wheels)
opencv-python-headless==4.8.1.78
numpy==1.24.3
Pillow==10.1.0
scikit-image==0.22.0
scipy==1.11.4

# Face Detection & Recognition (Alternative to dlib)
# Using mediapipe and face_recognition with pre-compiled wheels
mediapipe==0.10.8
face-recognition-models==0.3.0

# Deep Learning Frameworks (CPU/GPU compatible)
torch==2.1.1
torchvision==0.16.1
# tensorflow==2.15.0  # Optional - can be added if needed

# Face Detection Libraries (Lightweight alternatives)
facenet-pytorch==2.5.3
mtcnn==0.1.1

# Image Quality & Enhancement
# basicsr==1.4.2  # May have build issues, using alternative
# realesrgan==0.3.0  # May have build issues, using alternative
# gfpgan==1.3.8  # May have build issues, using alternative

# API & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
python-dotenv==1.0.0
pyjwt==2.8.0
cryptography==41.0.7

# Monitoring & Logging
prometheus-client==0.19.0
structlog==23.2.0
python-json-logger==2.0.7

# Database & Caching
redis==5.0.1
motor==3.3.2
aiofiles==23.2.1

# HTTP & Utilities
httpx==0.25.2
aiohttp==3.9.1
requests==2.31.0
tenacity==8.2.3

# Testing & Development
pytest==7.4.3
pytest-asyncio==0.21.1
